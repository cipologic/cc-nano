name: Test Linux (glibc 2.28+) Build

on:
  workflow_dispatch:

jobs:
  build-linux:
    name: Build compatible Linux binary (glibc 2.28)
    runs-on: ubuntu-latest
    container:
      image: almalinux:8
    steps:
      - name: Install Python 3.12 and tools
        run: |
          dnf install -y epel-release
          dnf install -y python3.12 python3.12-pip python3.12-devel gcc gcc-c++ make git
          ln -sf /usr/bin/python3.12 /usr/local/bin/python3
          ln -sf /usr/bin/pip3.12 /usr/local/bin/pip3

      - uses: actions/checkout@v6

      - name: Install Poetry and dependencies
        run: |
          pip3 install --upgrade pip
          pip3 install poetry
          poetry config virtualenvs.create true
          poetry install --no-interaction --no-root

      - name: Build with PyInstaller
        run: poetry run pyinstaller --clean --onefile --name cc-nano src/cc_nano/tui/app.py

      - name: Rename binary
        run: mv dist/cc-nano dist/cc-nano-linux

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: cc-nano-linux
          path: dist/cc-nano-linux